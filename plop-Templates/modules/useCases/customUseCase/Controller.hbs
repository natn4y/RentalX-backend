import { Request, Response } from 'express';
import { {{properCase useCaseName}}UseCase } from './{{properCase useCaseName}}UseCase'

class {{properCase useCaseName}}Controller {
  constructor(private {{camelCase useCaseName}}UseCase: {{properCase useCaseName}}UseCase) {}

  handle(request: Request, response: Response): Response {
    const { param } = request;

    this.{{camelCase useCaseName}}UseCase.execute({ param })

    console.log(param);

    return response.send();
  }
}

export { {{properCase useCaseName}}Controller }